# Copy this file to `.env.local` for local development and fill in your API key.
# Keep this file out of source control; `.env.local` is gitignored by Next.js by default.

# Google GenAI / Gemini API key (server-side only)
GEMINI_API_KEY=

# Optional: model to use (defaults to gemini-3-flash)
GEMINI_MODEL=models/gemini-3-flash-preview
